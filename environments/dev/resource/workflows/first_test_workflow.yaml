main:
  params:
    - args
  steps:
    - run_bigquery_job:
        call: callBQStoredProc
        args:
          stored_proc_name: '`"${var.project_id}".dataset1.new_proc`'
          project_id: "${var.project_id}"
